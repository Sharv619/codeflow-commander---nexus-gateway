# Set this to the backend host used by the Vite dev server proxy.
# For containerized override (frontend runs inside Docker), use:
# VITE_API_PROXY=http://backend:3001

# For Windows host where the backend runs on the host and the frontend runs in a container, use:
# VITE_API_PROXY=http://host.docker.internal:3001

VITE_API_PROXY=http://backend:3001

# Gemini API key used by the frontend build and runtime (do NOT commit your real key)
# Rename this file to ".env" and set values before running docker-compose or local dev
GEMINI_API_KEY=your_gemini_api_key_here
