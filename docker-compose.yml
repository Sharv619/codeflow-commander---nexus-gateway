version: '3.8'
services:
  backend:
    build:
      context: .
      dockerfile: server/Dockerfile
    image: nexus-gateway-backend:latest
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: nexus-gateway-frontend:latest
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped
